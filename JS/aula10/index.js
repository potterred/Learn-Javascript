const nome ='Morgana'; // string
const nome1 ="Morgana"; // string
const nome3 = `Morgana`; //string
const num1 = 10; //number
const num2 = 10.52; // number
let nomeAluno; // undefined -> não aponta pra local nenhum na memoria
let sobrenomeAluno =null; // Nulo -> não aponta pra local nenhum na memória
const aprovado = false; //Bolean = true, false (lógico)

let a = 2;
const b = a;
console.log(a, b); // 2, 2

a = 3;
console.log(a,b) // 3, 2
